# Desktop application dependencies
# 
# SECURITY NOTE: For release builds, use requirements-desktop.lock instead.
# The lockfile contains hash-pinned exact versions to prevent supply chain attacks.
# 
# To regenerate the lockfile after updating these requirements:
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file=requirements-desktop.lock requirements-desktop.txt
#
defusedxml>=0.7.1
Pillow>=12.3.0,<13.0
PySide6>=6.11.2,<7.0
PyInstaller>=6.22.2,<7.0
